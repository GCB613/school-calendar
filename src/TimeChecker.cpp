#include "TimeChecker.h"
#include "SchoolSettings.h"

bool TimeChecker::schoolIsInSession(time_t epoch) const
{
	// check to see if the given epoch is during school hours and not on a weekend or holiday

	return true;
}
